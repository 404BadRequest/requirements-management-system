// TODO: Implementar con tabla futura `profiles`.
// Campos sugeridos: id, name, hourly_rate (numeric), rate_currency (text), active (bool), created_at, updated_at.
// Métodos: getAll, create, update, delete (paridad con ProfilesRepository y settings).

export class SupabaseProfilesRepository {}
