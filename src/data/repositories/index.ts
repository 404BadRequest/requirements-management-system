/**
 * Punto de entrada legado: el acceso a datos está en `./server-db` (marcado `server-only`).
 * Importa siempre `@/data/repositories/server-db` desde Server Components o Server Actions.
 */
export {};
